import {t} from '../i18n.js';
export function renderHome(state,{navigate}){const app=document.querySelector('#app');app.innerHTML=`<section class="hero"><p class="eyebrow">CONNECTED NAVIGATION · V3</p><h1>${t('home.title')}</h1><p class="lede">${t('home.lede')}</p></section><section class="privacy-panel" aria-labelledby="privacy-title"><h2 id="privacy-title">${t('home.privacyTitle')}</h2><p>${t('home.privacy')}</p></section><section class="action-grid" aria-label="Whole Story actions">${card('continue',t('home.continue'),'Return to the most recent story you explicitly saved.')}${card('intake',t('home.startNew'),'Tell it once in your own words.')}${card('guided',t('home.guided'),'Optional prompts across the whole person.')}${card('next-step',t('home.next'),'See the one action that matters first.')}${card('connections',t('home.connections'),'Track resources and referrals you chose.')}${card('timeline',t('home.timeline'),'See what changed without retelling everything.')}${card('talk',t('home.talk'),'Start voice-first and use read-aloud where supported.')}</section><section class="row admin-links"><button class="secondary" data-view="navigator">Navigator mode</button><button class="secondary" data-view="admin">Local admin</button></section>`;app.querySelectorAll('[data-view]').forEach(b=>b.addEventListener('click',()=>navigate(b.dataset.view)))}function card(view,title,desc){return`<button class="action-card" type="button" data-view="${view}"><span class="action-arrow" aria-hidden="true">→</span><strong>${title}</strong><span>${desc}</span></button>`}
