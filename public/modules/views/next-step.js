import {loadDemoResources,matchResources,resourceStatusCopy,directoryStatus} from '../resources.js';
import {buildSharePackage,consentWithSelection,describeSharePackage} from '../consent.js';
import {exportHandoffText,downloadText} from '../export-import.js';
import {createReferral} from '../referrals.js';
import {saveReferral,saveLiveCaseMirror} from '../repositories.js';
import {personClient} from '../v4-client.js';
export async function renderNextStep(state,{navigate,patchState}){const app=document.querySelector('#app');if(!state.route||!state.activeStory){navigate('intake');return}let resources=state.resources;try{if(!resources.length)resources=await loadDemoResources()}catch{resources=[]}const alreadyTried=state.activeStory?.alreadyTried||[];const matches=matchResources({resources,signals:state.activeStory.signals,serviceArea:state.profile?.serviceArea||'',language:state.profile?.preferredLanguage||'en',alreadyTried});patchState({resources,matches});app.innerHTML=`<section class="page-head"><div><p class="eyebrow">MY NEXT STEP</p><h1>${esc(state.route.title)}</h1><p class="lede">${esc(state.route.why)}</p></div><button class="secondary" data-home>Home</button></section><section class="route-card"><strong>${esc(state.route.owner)}</strong><ol>${state.route.actions.map(a=>`<li>${esc(a)}</li>`).join('')}</ol></section><section><h2>Possible connections</h2><p class="muted">These resources <strong>may be a fit</strong>. Matching never guarantees eligibility, availability, funding, or acceptance.</p><p class="muted">${esc(directoryStatus())}</p><div class="field"><label for="resourceSearch">Search these possible connections</label><input id="resourceSearch" placeholder="Search by name or need"></div><div class="resource-grid" id="resourceGrid">${matches.length?matches.map(m=>`<article class="resource-card" data-resource-card><span class="pill ${m.resource.isDemo?'demo':''}">${esc(resourceStatusCopy(m.resource))}</span><h3>${esc(m.resource.name)}</h3><p>${esc(m.resource.description||'')}</p><p class="muted">${esc(m.reasons.join(' · ')||'Potential domain match')}</p>${m.alreadyTried?'<p><strong>Already tried</strong> — shown lower so you can avoid a dead end.</p>':''}<button class="primary" data-resource="${m.resource.id}">Prepare a referral</button></article>`).join(''):'<p>No matching cached resources are available. You can still use the named next-step owner above.</p>'}</div></section><section id="preview"></section>`;app.querySelector('[data-home]').onclick=()=>navigate('home');const search=app.querySelector('#resourceSearch');if(search)search.oninput=()=>{const q=search.value.trim().toLowerCase();app.querySelectorAll('[data-resource-card]').forEach(card=>card.hidden=q&&!card.textContent.toLowerCase().includes(q))};app.querySelectorAll('[data-resource]').forEach(b=>b.onclick=()=>preview(b.dataset.resource));function preview(id){const resource=resources.find(r=>r.id===id);const consent=consentWithSelection(['signals','priority']);const pkg=buildSharePackage({story:state.activeStory,signals:state.activeStory.signals,priority:state.route,profile:state.profile||{},consent,alreadyTried});const preview=app.querySelector('#preview');preview.innerHTML=`<section class="card stack"><div><p class="eyebrow">REFERRAL PREVIEW</p><h2>Review exactly what will be shared</h2><p><strong>${esc(resource.name)}</strong></p></div><ul>${describeSharePackage(pkg).map(x=>`<li>${esc(x)}</li>`).join('')}</ul><details><summary>Preview data</summary><pre>${esc(JSON.stringify(pkg,null,2))}</pre></details><label><input type="checkbox" id="confirmShare"> I reviewed this limited share package.</label><div class="row"><button class="primary" id="createReferral">${resource.partnerOrgId?'Send to connected partner':'Create legacy local referral draft'}</button><button class="secondary" id="copyHandoff">Copy handoff</button><button class="secondary" id="shareHandoff">Share handoff</button><button class="secondary" id="downloadHandoff">Download handoff text</button><button class="secondary" id="cancelPreview">Cancel</button></div><p class="muted">Connected partner referrals send only after this explicit review. Resources without a connected partner remain local legacy drafts and are not transmitted.</p></section>`;preview.querySelector('#cancelPreview').onclick=()=>preview.replaceChildren();const handoff=()=>exportHandoffText(pkg);preview.querySelector('#copyHandoff').onclick=async()=>{await navigator.clipboard.writeText(handoff())};preview.querySelector('#shareHandoff').onclick=async()=>{if(navigator.share){try{await navigator.share({title:'Whole Story handoff',text:handoff()})}catch{}}else{await navigator.clipboard.writeText(handoff())}};preview.querySelector('#downloadHandoff').onclick=()=>downloadText('whole-story-handoff.txt',handoff());preview.querySelector('#createReferral').onclick=async()=>{if(!preview.querySelector('#confirmShare').checked)return;if(resource.partnerOrgId){try{const serverPreview=await personClient.previewCase({story:{id:state.activeStory.id,text:state.activeStory.rawStory||state.activeStory.story||''},signals:state.activeStory.signals||[],priority:state.route,profile:state.profile||{},consent:{shareScope:'selected_fields',selectedFields:['signals','priority']},resourceId:id,partnerOrgId:resource.partnerOrgId});const created=await personClient.createLiveCase({sharePackage:serverPreview.sharePackage,shareScope:serverPreview.shareScope,personDisplayName:serverPreview.personDisplayName,preferredContactMethod:serverPreview.preferredContactMethod,storyId:state.activeStory.id,resourceId:id,partnerOrgId:resource.partnerOrgId,createdBy:{type:'person',id:null,displayName:state.profile?.displayName||null}});await saveLiveCaseMirror({...created.case,accessSecret:created.accessSecret,lastConfirmedAt:new Date().toISOString()});navigate('connections');return}catch(e){const p=document.createElement('p');p.className='notice';p.textContent=e.code==='offline'?'Offline: this referral was not sent. Reconnect and explicitly try again.':'The partner handoff was not sent. Review and try again.';preview.append(p);return}}const referral={...createReferral({storyId:state.activeStory.id,resourceId:id,sharePackage:pkg}),legacyLocalReferral:true};await saveReferral(referral);patchState({referrals:[...state.referrals.filter(r=>r.id!==referral.id),referral]});navigate('connections')}}}
function esc(s=''){return String(s).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}
