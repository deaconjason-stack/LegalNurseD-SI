export function createRateLimiter({limit=10,windowMs=60_000}={}){const map=new Map();return{take(key,now=Date.now()){const t=typeof now==='number'?now:new Date(now).getTime();let r=map.get(key);if(!r||t-r.start>=windowMs)r={start:t,count:0};r.count++;map.set(key,r);return r.count<=limit?{allowed:true,retryAfterSeconds:0}:{allowed:false,retryAfterSeconds:Math.max(1,Math.ceil((windowMs-(t-r.start))/1000))}},clear(){map.clear()}}}
